<?php

namespace Database\Seeders;

use App\Models\Group;
use App\Models\Lecture;
use App\Models\Student;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {
//         \App\Models\Student::factory(15)->create();
//         \App\Models\Group::factory(3)->create();
//         \App\Models\Lecture::factory(10)->create();
//        foreach (Lecture::all() as $lecture) {
//            $students = Student::inRandomOrder()->take(rand(1,30))->pluck('id');
//            $lecture->students()->attach($students);
//        }
//         \App\Models\Plan::factory(3)->create();
    }
}
